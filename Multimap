multimap<int,int> mp
  - implements red black tree,
  - allows multiple keys,
  - mp.insert(key, val) 
  - multimap<int,int> - ascending order,
  - multimap<int,int,greater<int>> - descending order,
  - mp[key] = val is not valid,
  - mp.count(key) - returns the occurences of keys,
  - mp.erase(key) - deletes all the occurences of key,
  - mp.lower_bound(key) - returns the add of key if present : returns the add just above the key,
  - mp.upper_bound(key) - returns the add just above the key whether it is present or not,
  - both returns mp.end() if key is greater than the last valuein map,
  - mp.equal_range(key) - returns pair of iterators of first occurence of key and add of key just after the last occurences,
  - mp.begin(),
  - mp.end(),
  - mp.rbegin(),
  - mp.rend(),
  - mp.size(),
  -mp.empty(),
